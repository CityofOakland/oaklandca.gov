{% extends '_layout' %}

{% block content %}
  <section class="bg-white">
    <div class="container section-container">
      <h2 class="mt-0">We apologize, the page you were looking cannot be found.</h2>
      <p>Try returning to the homepage or searching for what you need.</p>
      <form action="{{ url('search') }}">
        <div class="sm:flex max-w-md">
          <input class="w-full mb-4 sm:flex-1 sm:mb-0 p-4 border-shark border rounded-none appearance-none" name="keywords" placeholder="How can we help?" type="search" value="">
          <button class="w-full sm:w-auto btn border-l-0 border-shark border" type="submit">Search</button>
        </div>
      </form>
    </div>
  </section>
{% endblock %}
