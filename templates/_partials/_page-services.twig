{% if entry.pageServices | length %}
  <section class="bg-rotate">
    <a id="page-services" class="anchor-link"></a>
    <div class="container mx-auto p-8 py-16">
      {% include '_partials/_elements/_module-header' with {
        headerText: headerText ?? "Featured Services",
        linkText: linkText ?? "All Services",
        linkUrl: linkUrl ?? "/services/" ~ entry.type
      } %}
      <div class="card-grid grid-equal-height mt-8">
        {% for entry in entry.pageServices.all() %}
          {% include '_partials/_elements/_title-cards' %}
        {% endfor %}
      </div>
    </div>
  </section>
{% endif %}
